name: GA_EX_011
on: issues
jobs:
    issues:
        runs-on: ubuntu-latest
        steps: 
            - name: Github Issues Output
              run: echo "******* Opened ISSUE *******"
            - name: Issues Header
              run: echo "Title: ${{ toJson(github.issue.title) }}" 
            
            # - name: Github JSON Content
            #   run: echo "${{ toJson(github) }}"